# 후투티 — 변경 내역

## 2026-08-16 (5) — 체크 항목의 갈래 표시 제거

증상 체크 항목 앞에 붙던 (생각)·(행동)·(감정)·(신념)·(신체) 표시를 없앴다.

**다섯 갈래 구분은 맨 처음 소견을 나눠 적게 하는 용도로만 남긴다**(사용자 결정).
체크 단계에서는 항목 문구만 읽고 판단하는 편이 방해가 적다는 판단이다. 소견 입력의
다섯 칸은 그대로이며, 그쪽이 이 개념이 실제로 값을 하는 자리다.

`js/symptom-tags.js`(문구에서 갈래를 판별하던 규칙 표)는 쓰이는 곳이 없어져 삭제했다.
관련 스타일과 스크립트 연결도 함께 정리했다. 되살릴 일이 생기면 git 이력에 남아 있다.

## 2026-08-16 (4) — AI 응답 잘림 표시 · 안내문 현행화

### 답이 잘렸는지 확실하게 알린다

분량 제한에 걸려 답이 중간에 끊겼는지 **엔진이 알려주는 사실로** 판단한다.
문장이 어떻게 끝났는지(마침표 여부)로 추정하지 않는다 — `~다.`로 끝나도 뒤가
잘렸을 수 있어 신뢰할 수 없다.

| 엔진 | 판단 근거 |
|---|---|
| Ollama | 응답의 `done_reason`이 `length`면 잘린 것, `stop`이면 스스로 끝낸 것 (실측 확인) |
| 카나나 | 흘러온 토큰 조각 수가 상한(900)에 닿았는지로 판단 |

잘렸을 때만 **"뒷부분 잘림"** 표시와 안내를 띄운다. **판단할 근거가 없으면 아무 말도
하지 않는다** — 멀쩡한 결과를 잘렸다고 하면 상담자가 공연히 의심하게 된다.

### about.html 안내문을 지금 구조에 맞게 다시 썼다

기존 문구는 "메인 화면의 채팅창에서 대화하며 후보를 좁혀간다"를 전제로 쓰여 있어
**사실과 달랐다.** 지금은 문항·채점·기간 판정에 AI가 전혀 관여하지 않는다.

- 제목을 "AI가 하는 일과 하지 않는 일"로 바꾸고, **후보를 좁히는 것은 규칙이고 AI는
  이미 나온 결과를 정리할 뿐**임을 앞세웠다
- 카나나 중심으로 서술. Ollama는 참고 한 줄로 줄이고, **웹으로 접속하면 브라우저 보안
  정책상 Ollama에 접근할 수 없어 항상 카나나가 쓰인다**는 사실을 명시했다
  (배포 사이트에서 `http://localhost:11434` 요청이 차단되는 것을 실측 확인)
- 느릴 수 있다는 점, 진행이 글자로 보인다는 점, 취소할 수 있다는 점을 안내
- 잘림 표시의 의미를 설명

### 참고 — Ollama 코드는 그대로 둔다

배포 사이트에서 못 쓰는 것은 맞지만, **로컬 주소로 열면 실제로 동작하며 훨씬 빠르다**
(실측: `http://localhost:8777`에서는 모델 목록이 정상 조회됨). 제거하면 그 이득이
사라지고 나중에 호스팅을 옮길 때 다시 만들어야 하므로 자동 감지를 유지한다.

## 2026-08-16 (3) — 불러온 뒤 수정해도 반영되지 않던 문제

저장한 파일을 불러와 이어서 작업할 때, **고친 내용이 판정에 반영되지 않았다.**

- **기간·빈도를 다시 물어볼 방법이 없었다.** 체크를 고쳐 유력한 진단이 바뀌어도
  확인 대상 목록이 예전 상태로 굳어 있었고, 정량 단계로 돌아갈 버튼도 없었다.
  → **“기간·빈도 다시 확인하기”** 버튼을 두었다. 누르면 지금 체크 상태로 대상을
  다시 뽑는다. 대상이 달라졌으면 그 사실도 함께 알린다.
- **AI 정리가 낡은 채로 남았다.** 체크를 고쳐도 예전 설명이 그대로 있어 오해를 부를
  수 있었다. → 요약을 만든 시점의 입력과 지금 입력이 다르면 **“갱신 필요”** 표시를
  띄우고 흐리게 보여 준다.
- **문답이 끝난 뒤 체크를 고치면 화면을 다시 그리도록** 했다(위 안내를 갱신하기 위함).
  대신 **다시 그릴 때 보던 위치를 지킨다** — 맨 아래를 보고 있었으면 따라가고,
  위쪽에서 고치는 중이었으면 그 자리에 머문다. 무조건 맨 아래로 보내던 동작이
  "체크하면 화면이 튄다"의 또 다른 원인이었다.

## 2026-08-16 (2) — AI 응답이 멈춘 것처럼 보이던 문제 · 체크 시 화면 흔들림

실사용에서 나온 보고 두 건을 고쳤다.

### AI 응답이 멈춘 것처럼 보이고, 이후 질문에 답이 없었다

세 가지가 겹친 문제였다.

1. **진행 표시가 없었다.** 응답을 다 만들 때까지 화면에 아무 변화가 없어, 브라우저 안
   AI처럼 수 분이 걸리는 경우 멈춘 것으로 보였다.
   → **토큰이 생성되는 대로 화면에 흘린다.** 점이 깜빡이고 문장이 실시간으로 쌓인다.
2. **생성 중 입력한 질문이 조용히 버려졌다.** `if (running) return`으로 무시되어
   말풍선은 남는데 답이 영영 오지 않았고, 첫 요청이 끝나지 않으면 이후 질문도 계속
   같은 이유로 사라졌다.
   → 생성 중에는 **입력창을 잠그고**, 그래도 들어오면 대화에 넣지 않고 이유를 알린다.
3. **취소할 방법이 없었다.** → **취소 버튼**을 두었다. 누르면 즉시 멈추고 기록된다.

실패해도 조용히 끝나지 않고 사유를 화면에 남기며, "다시 정리받기"로 재요청할 수 있다.

### 증상을 체크할 때 화면이 흔들렸다

- **스크롤 상자가 이중이었다.** 체크리스트가 자체 스크롤(460px) 안에 있고 그것이 다시
  스크롤되는 대화 영역 안에 있어, 휠로 넘기다 끝에 닿으면 바깥으로 스크롤이 넘어가
  화면이 통째로 튀었다 → **안쪽 스크롤을 없애 스크롤 축을 하나로** 했다.
- **배지 문구가 바뀌며 너비가 변했다**(미충족 49px → 부분 일치 20% 88px)
  → 최소 너비를 고정해 글자가 바뀌어도 자리가 흔들리지 않는다.

### 겸사겸사 확인된 것

배포 사이트에서 **멀티스레드가 실제로 켜져 있음을 처음 확인했다** —
`crossOriginIsolated=true`, `SharedArrayBuffer` 사용 가능, COOP/COEP 헤더 적용됨.
2026-07-19에 넣은 `coi-serviceworker`가 의도대로 동작하고 있으며, 그동안 "미검증"으로
남아 있던 항목이 해소됐다. **즉 브라우저 AI는 이미 최선의 경로로 돌고 있고, 그런데도
느린 것은 모델 자체의 한계다.** 속도를 실제로 바꾸려면 Ollama 설치가 답이다.

AI 응답 상한은 설명 900토큰(한글 약 1,100자), 후속 질문 700토큰이다. 요구하는 형식이
다 써도 550자 정도라 여유가 있어 그대로 둔다. 생성 시간은 상한이 아니라 **실제로 쓴
분량**에 비례하므로, 상한을 낮춰도 빨라지지 않는다.

## 2026-08-16 — 알고리즘 문답 화면 + 기간·연령·빈도·기능손상 자동 판정

7월 19일 이후 작업을 한 번에 반영한다. 메인 화면이 **AI 채팅 → 알고리즘 문답**으로 바뀌고,
그동안 채점에서 빠져 있던 **기간·연령·빈도·기능손상 기준이 판정에 들어왔다.**

기획·설계 문서: `docsPlan/guided-intake-wizard/` · `docsPlan/quantified-criteria/`
검토용 자료: `docsPlan/quantified-criteria/검토용-문항과규칙.md`

---

### 왜 바꿨나

**문제 1 — 채팅이 느리고 죽었다.** 매 턴마다 진단 사전 전체와 누적 대화를 다시 보내는
구조라, 결과 1건에 AI를 최대 6회 호출했고 대화가 길어지면 문맥 한도(4096)를 넘어
`(ABORT)`로 죽었다. 응급 가드(2026-07-19)는 증상만 눌렀을 뿐 구조를 바꾸지 않았다.

**문제 2 — 증상 개수만 셌다.** `js/data.js`의 기간·배제 기준이 줄글이라 자동 채점에서
빠져 있었다(README 명시). 그래서 **우울 증상 5개가 체크되면 그것이 2주짜리든 이틀짜리든
똑같이 "기준 충족 가능성"으로 표시됐다.** DSM-5-TR은 2주 이상을 요구하는데도 그랬다.

---

### 바뀐 것

**메인 화면 — 알고리즘 문답 (`index.html`)**

대화 레이아웃은 그대로 쓰되 AI가 말을 지어내는 대신 **알고리즘이 정해진 문항을 말풍선으로
던진다.** 답한 문항은 위에 쌓여 지나온 판단을 되짚을 수 있다.

- 소견을 **생각·행동·감정·신념·신체 다섯 갈래**로 나눠 받는다
- 소견에 걸린 영역은 **하나씩 자세히**, 나머지는 **한 목록으로 묶어 빠르게** 확인한다
- 답변은 **예 / 아니오 / 확인 안 됨** 3가지. "확인 안 됨"은 "아니오"와 다르게 처리된다
- 정밀 체크리스트가 대화 안 말풍선으로 들어온다
- AI 호출은 **결과당 1회**. 누적되는 대화가 없어 `(ABORT)`가 구조적으로 발생하지 않는다
- 입력창은 **문답이 끝난 뒤** 살아나 AI에게 추가 질문을 받는다

**기간·연령·빈도·기능손상 자동 판정 (신규)**

81개 진단 중 **45개**에 대해 다섯 축을 판정한다. 판정은 5가지 상태로 표시된다 —
충족 / 미충족 / 미확인 / 직접 확인 필요 / (규칙 없음).

- **미입력은 절대 "충족"이 되지 않는다** (전수 검사로 확인)
- 미충족이어도 **목록에서 제거하지 않는다.** 순위와 표시에만 반영한다
- 자동 배제(exclusion) 기능은 만들지 않았다 — 감별진단은 임상가의 몫이다
- 계산할 수 없는 중첩 조건("2개월 이상 증상 없는 기간이 없음" 등)은 임의 판정하지 않고
  **"직접 확인할 것"으로 안내**한다

**문진 문항 22 → 23개**

ADHD를 단독 문항으로 분리했다. 가장 흔한 의뢰 사유이고 다른 신경발달장애와 증상 성격이
다르다. 81개 진단이 23개 문항에 빠짐없이·중복 없이 배분된다(자동 검사).

**증상 항목 라벨**

체크 항목 앞에 (생각)·(행동)·(감정)·(신념)·(신체) 표시가 붙는다. 395개 중 199개(50%)에
붙고 애매한 것은 무라벨이다. **표시일 뿐 어떤 항목도 숨기지 않으며 채점과 무관하다.**
탭 필터로 만들지 않은 것은 의도적이다 — 분류가 틀린 항목이 숨겨지면 놓치게 된다.

**결과 순위 기준 변경**

"충족 여부 → **체크된 항목 수** → 충족 비율" 순으로 바꿨다. 이전 기준(충족 → 비율)에서는
요구 항목이 1개뿐인 망상장애가 9개 중 5개를 요구하는 주요우울장애보다 앞섰다.

**저장·불러오기 (신규)**

"저장"을 누르면 **읽기용 `.txt`와 이어하기용 `.json` 두 파일**이 함께 만들어진다.
웹으로 접속했으면 폴더를 직접 고를 수 있다. `.json`을 "불러오기"로 열면 소견·문항 응답·
체크·정량 입력·AI 대화까지 복원되어 **이어서 작업할 수 있다.**

> 이 파일에는 내담자 증상 정보가 담긴다. 지금까지 이 도구는 탭을 닫으면 아무것도 남지
> 않았으므로, 저장은 그 원칙에서 벗어나는 선택이다. 화면에 보관·삭제 주의를 명시했다.

**ICD-11 페이지 수정**

파일을 직접 열면(`file://`) 149개 항목이 하나도 표시되지 않던 문제를 고쳤다.
외부 데이터를 읽어오는 방식이 그 환경에서 차단되던 것이라, `data.js`와 같은 방식으로 옮겼다.

**자유 대화 보존 (`chat.html`)**

이전 채팅 화면을 지우지 않고 남겼다. 되돌릴 경로이자 자유 서술이 필요한 경우를 위한 것이다.
메인 화면이 후속 질문 기능을 흡수했으므로 메뉴에서는 뺐다.

---

### 검증

| 항목 | 결과 |
|---|---|
| 채점 로직 분리 동치 | 81개 진단 × 1002개 조합 = **81,162 케이스 완전 일치** |
| 규칙 수치 원문 대조 | **243건 통과** — 원문에 없는 수치 0건 |
| 판정 엔진 단위 테스트 | **54건 통과** (경계값·소아 분기·예외·단위 모호성) |
| 미입력 안전성 | **입력 없이 충족되는 진단 0개, 축 0개** |
| 문항 커버리지 | 81개 진단이 23개 문항에 빠짐없이·중복 없이 |
| 문답 화면 전 흐름 | 통과 (소견→문항→체크→정량→저장→불러오기→복원) |
| AI 없이 완주 | 통과 (`file://`에서 AI만 빠지고 나머지 정상) |
| 기존 화면 회귀 | `checklist.html`·`chat.html` 무영향 |

---

### 아직 확인되지 않은 것

- **실제 AI 설명 생성** — 모델 1.4GB 다운로드와 수 분의 추론이 필요해 자동 테스트에서
  제외했다. 실사용으로 확인해야 한다.
- **문항 23개와 규칙 45건의 임상적 타당성** — 규칙이 `data.js` 원문과 일치함은 증명했으나
  **원문 자체가 옳은지는 증명하지 못한다.** `data.js`는 임상 전문가 미검증 재서술 자료다.
  검토용 자료를 `docsPlan/quantified-criteria/검토용-문항과규칙.md`에 정리해 두었다.

### 남은 리스크

- 소견 키워드 매칭은 한국어 어미 변화에 약하다. 다만 **순서와 묶음에만 영향**을 주고
  문항 자체는 모두 확인하므로 결과 정확도에는 영향이 없다.
- APA의 DSM-5-TR Level 1 Cross-Cutting Symptom Measure 도입은 보류 상태다.
  배포 조건을 APA에 문의해야 한다(`docsPlan/quantified-criteria/requirements.md` §8).

---

## 2026-07-19 — 카나나 문맥 예산 가드 · coi-serviceworker

`js/context-budget.js` 신설(토큰 근사·오래된 턴 탈락·진단 전문 동적 축소)과
`coi-serviceworker` 도입(GitHub Pages에서 멀티스레드 활성화 시도).
상세 = `docsPlan/ai-token-speed-accuracy/DECISION.md`.
