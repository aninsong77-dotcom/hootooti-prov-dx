# AI 개선 종합 결정안 — 토큰 부족·속도·가진단 정확도

작성: 2026-07-19. 지금까지의 외부 조언 3건 + 코드 실측 검증을 종합한 **결정 문서**.
아직 구현 착수 전 — 사용자 승인 후 작업 시작.

관련 문서: `docsPlan/kanana-context-limit-crash/HANDOFF.md`(크래시 조사),
메모리 `project_kanana_abort_crash.md`(07-19 실측 포함).

---

## 0. 문제 정의 (세 가지)

| 문제 | 증상 | 뿌리 원인 (실측 확인됨) |
|---|---|---|
| A. 토큰 부족 | 답변 잘림 + 대화 길어지면 `(ABORT)` 크래시 | N_CTX=4096은 입력+출력 합산 예산인데, 사전(기준 전문)+전체 대화 히스토리가 예산을 잠식 (`js/ai.js:1057` 주석) |
| B. 속도(버벅거림) | 응답 생성이 느림 | ① 싱글스레드 실측 확정 — 배포 헤더에 COOP/COEP 없음(07-19 curl 실측) ② 프롬프트가 길어 prefill(입력 처리)도 느림 |
| C. 정확도 | 소형 모델(카나나 2.1B)의 환각·비일관 추론 | 작은 모델에게 상태 유지·다단계 추론까지 맡기는 현재 구조 |

## 1. 결정 사항

### A. 토큰 부족 → "짐 줄이기" 3단계

1. **[단기] 경량 안전장치** — 프롬프트 조립 시점 토큰량 추정(글자수 근사) → 한도 근접 시 경고 + Ollama 유도 → 초과 시 가장 오래된 턴부터 단순 탈락(요약 압축 ❌ — 아래 3에서 버려질 코드라 과투자 금지).
2. **[단기] 진단 전문 가지치기** — FULL_DETAIL_DIAGNOSES=6 고정을 동적으로: 대화가 진행될수록(후보가 좁아질수록) 전문 개수를 줄이고, 배제된 진단은 명단에서 제거. 회수한 토큰 = 답변 공간.
3. **[중기·최종 그림] 임상 추론 루프 재설계** — "상태·채점은 JS, LLM은 얇게". 진단별 점수표를 JS 객체로 관리(기존 `main.js` `diagnosisScore`/`groupSatisfied` 재사용), 매 호출은 "현재 입력 + 확인할 기준 몇 개"만 전달 → 히스토리 누적 문제가 구조적으로 소멸. 별도 트랙으로 `docsPlan/`에 기획부터.

**기각(실측 근거)**: nPredict 상향(이미 1800, "올려도 잘림" 실측 `ai.js:275`), n_ctx 8192(GPU 드라이버 행 + CPU 수십 분 실측 `ai.js:33,836`), 모델 교체(Qwen3-4B AMD WebGPU 크래시 이력 + 카나나 한국어 특화).

### B. 속도 → 모델 교체 없이 2가지

1. **coi-serviceworker 도입** — GitHub Pages는 `_headers` 무시 → crossOriginIsolated=false → SharedArrayBuffer 불가 → wllama 싱글스레드(실측 확정). coi-serviceworker로 COI를 켜면 멀티스레드 wasm 활성화 가능. **도입 시 필수 테스트**: HF 모델 다운로드(1.4GB)·jsDelivr가 COI 아래에서 살아남는지 실기기 확인 + wllama가 실제 멀티스레드 빌드를 선택하는지 콘솔 확인.
2. **A의 토큰 다이어트가 곧 속도 개선** — CPU 추론에서 prefill 시간은 프롬프트 길이에 비례. 프롬프트가 절반이면 첫 글자 나오기까지의 대기도 크게 준다.

### C. 정확도 → "LLM은 얇게" + 그라운딩 강화

1. **그라운딩 유지·강화**(이미 구현됨) — data.js 기준 전문을 프롬프트에 주입, 모델 기억이 아니라 눈앞 자료로 판단. C의 모든 개선은 이 위에 쌓는다.
2. **임상 추론 루프에서 LLM의 일은 딱 3개** — ① 자유 서술에서 증상 추출 → data.js 항목 ID로 매핑 ② 다음 확인할 기준을 자연스러운 질문 문장으로 변환 ③ 최종 후보의 근거 설명. 점수 계산·가설 유지·후보 갱신은 전부 결정론적 JS.
3. **추출 매핑 신뢰성 확보(성패의 핵심)** — 출력 JSON 강제 + 코드가 실존 항목 키인지 검증(불일치 폐기) + wllama의 GBNF 문법 강제 샘플링 지원 여부 확인(되면 형식 깨짐 원천 차단).
4. **감별 질문 자동 선택** — 남은 유력 후보들을 가장 잘 갈라내는 기준을 다음 질문으로(기준 겹침 비교 코드로 자동화). 자동 채점에서 빠져 있는 `duration`(기간)·`other`(배제 기준) 필드를 질문 우선 소재로.
5. **출력 형식 제한** — 섹션별 개별 요청(기구현) 위에 "유력 진단 최대 3개, 근거 각 2줄 이내" 식 길이 제한 강화. 임상 참고 도구엔 장황함보다 구조화가 낫다.
6. **회귀 테스트 10~20케이스** — 입력 소견 → 기대 진단 후보. 채점은 결정론적이라 테스트 대상은 LLM 추출 단계. 기획과 병행해 케이스부터 축적.
7. **주의 2가지(설계 제약)** — 질문 루프를 심문처럼 만들지 않기(자유 서술이 주 통로, AI 질문은 제안 수준) / 공존진단 대비 복수 후보를 끝까지 유지(단일 수렴 ❌).

## 1.5 진행 현황 (2026-07-19 구현, 커밋 전)

- **트랙 1 (응급처치) 구현 완료**: `js/context-budget.js` 신설(토큰 근사·오래된 턴 탈락·전문 개수 동적화, 순수 모듈) + `js/ai.js` 카나나 호출 4곳 가드 연결(Ollama 경로는 의도적으로 제외) + `js/ai-ui.js` 안내 말풍선(near/trimmed 시 1회).
  검증: node 단위 테스트 19/19 PASS, ESM 문법 검사 통과, Playwright 브라우저 스모크(콘솔 에러 0, 채팅 UI 정상).
  미검증(실기기 필요): 실제 장시간 대화에서 ABORT 미발생 — 사용자 실사용 테스트로 판정.
- **트랙 3 (coi-serviceworker) 구현 완료**: `coi-serviceworker.min.js`(v0.1.7 MIT) 루트 배치 + `index.html` 첫 스크립트 연결.
  검증(Playwright, localhost): crossOriginIsolated=true · SharedArrayBuffer 사용 가능 · HF 모델 Range 다운로드 206 성공 · wllama ESM 로드 성공 · 폰트/체크리스트 페이지 무영향 · 콘솔 에러 0. wllama 3.5.1은 SAB 가용 시 자동 멀티스레드(n_threads=코어/2) — 코드 수정 불필요 확인.
  미검증(배포 후): GitHub Pages 실배포에서 동일 동작 + 실제 생성 속도(tokens/sec) 전후 비교.
- **트랙 2 (임상 추론 루프)**: 미착수 — 기획부터.

## 2. 실행 순서

| 순서 | 트랙 | 규모 | 내용 |
|---|---|---|---|
| 1 | kanana-context-limit-crash (기존) | 소 | A-1 안전장치 + A-2 가지치기 + C-5 길이 제한 |
| 2 | 임상 추론 루프 (신규, 기획부터) | 대 | A-3 + C-2~C-4, 회귀 케이스(C-6) 병행 축적 |
| 3 | 속도 (독립·저위험) | 소 | B-1 coi-serviceworker (1·2와 무관하게 진행 가능) |

## 3. 실패 시나리오와 플랜 B

각 트랙의 확실성 등급과, 효과 없을 때의 대안. **착수 전에 성공 판정 기준(측정법)을 정해두는 것이 핵심** — "효과 없음"을 감으로 판단하지 않는다.

| 트랙 | 확실성 | 성공 판정 기준 | 안 되면 (플랜 B) |
|---|---|---|---|
| 1. 응급처치 (한도 가드+가지치기) | 높음 — 단, 크래시 원인이 문맥 한도라는 것은 아직 **추정**(정황 증거, HANDOFF 참고) | 크래시 재현 시나리오(장시간 대화)에서 ABORT 0회 + 답변 잘림 감소 | 가드를 넣었는데도 크래시 → 원인 가설이 틀렸다는 **증거 확보**가 성과. 그때는 콘솔 로그 채집으로 진짜 원인 추적(디버그 트랙 전환). 확실한 최후 수단 = Ollama 경로(이미 구현됨, 큰 문맥·큰 모델 가능) |
| 2. 임상 추론 루프 | 중간 — 성패는 2.1B 모델의 증상 추출 정확도에 달림 (본 문서 C-3) | 회귀 케이스 10~20개에서 추출 정확도 측정 — **전면 전환 전에 추출 단계만 먼저 파일럿** | ① LLM 역할 더 축소: 기존 키워드 매칭을 추출기로, LLM은 질문 문장화·설명만 ② LLM 제안 + 키워드 교차 확인 하이브리드 ③ 추출만 Ollama(큰 모델)에 위임하는 옵션 ④ 최악: 체크리스트 수동 유지 + AI는 질문·설명 보조만 — 어느 경우든 점수판-JS 구조 자체는 유효 |
| 3. coi-serviceworker | 중간 — COI가 켜져도 속도 향상 폭은 실측해야 (메모리 대역폭이 병목이면 코어를 늘려도 이득 제한) | 도입 전후 tokens/sec 비교 + 모델 다운로드 정상 + wllama 멀티스레드 빌드 선택 확인 | 스크립트 한 줄이라 **제거하면 즉시 원상복구** — 매몰 비용 거의 0. 효과 미미하면 속도는 1번의 프롬프트 다이어트 효과에 의존 |

공통 안전판: 순서가 "싼 것 먼저"라 큰 공사(2번)는 파일럿 측정을 통과해야만 본격 착수 — 실패해도 잃는 것이 기획 문서와 테스트 케이스뿐이며, 이는 어떤 대안에서도 재사용된다.

## 4. 다음 할 일 (다음 세션 진입점 — 사용자가 "뭐 할 차례?"라고 물으면 여기부터)

1. **[관찰 중] 카나나 크래시 가드 실사용 검증** — 2026-07-19 배포. 1차 테스트에서 'near' 경고 직후 (ABORT) 재현 → 원인 가설(문맥 한도)은 확증됐으나 토큰 추정이 낙관적이었음이 실측됨. 같은 날 보수화 패치(한글 1.25자/토큰·오버헤드 16·85% 지점 선제 탈락, c1e8366) 배포. 이후 긴 대화에서 재현 없으면 합격, 또 죽으면 wllama tokenize()로 정확 측정 전환 검토(근사 포기).
2. **[다음 메인 작업] 트랙 2: 임상 추론 루프 기획** — `docsPlan/`에 새 트랙 폴더 만들고 requirements.md부터. 핵심 설계: "상태·채점은 JS(main.js diagnosisScore 재사용), LLM은 증상 추출·질문 문장화·근거 설명만"(§1-C). 착수 전 회귀 테스트 케이스 10~20개(입력 소견 → 기대 진단 후보) 축적 병행. 속도 문제(섹션별 요청이 매번 사전+대화를 다시 읽는 구조)의 근본 해결이 여기 달려 있다.

   **2026-07-19~20 사용자와 합의된 구체화** (기획 시 이 내용을 출발점으로):
   - **UX = 질문 주도 대화**: 임상가 진단 프로세스 순서대로 대화창에 질문이 뜨고 사용자가 답하는 흐름. 답변은 버튼(예/아니오/확인 안 됨)+자유 입력창 병행 — 버튼 답은 규칙 엔진이 0초 처리, 자유 서술만 LLM 추출 1회. 결과("한큐에")는 규칙 엔진 채점표 즉시 + LLM 설명 1단락. AI 호출이 턴당 6회→자유 입력 시 1회로 감소(속도 요구 충족).
   - **아키텍처**: 외부 조언(규칙 엔진+코드북) 검증 완료 — data.js 항목에 ID 부여(코드북화), 기준 데이터·채점기는 기존 것 재사용. 새로 만들 것은 LLM 추출과 결과 패널 UI뿐. 결과 패널은 "N개 중 M개 확인 ✓ + 근거 발화 + 미확인 항목 + 상담사 체크 수정(즉시 재채점)" 구조화. UI 뼈대·페이지 구성은 불변.
   - **주의(외부 조언의 오류 보정)**: 일렉트론 전제 ❌(웹 구조 유지), ICD-11 기준도 CDDG 원문은 WHO 저작권 — data.js처럼 재서술로. ICD/DSM 이중 대조는 기준 파일 2벌+매핑으로 가능(후순위 단계).
   - **모델 전략**: Ollama(큐웬3-4B) 감지 시 우선은 현행 유지 — 큐웬은 요약·설명에 강함. 카나나는 "설치 없는 기본", 큐웬은 "설치 시 정밀 모드" 이중 구조. 카나나 충분성은 추측 금지 — 테스트 문장 20개(부정문·간접표현·화자 구분 함정 포함)로 두 모델 추출 정확도 실측 후 결정. JSON 강제(Ollama format/스키마, wllama GBNF 확인) + 코드의 실존 ID 검증 필수.
   - **규모 견적**: 기획1 + 코드북화1 + 추출실험1~2 + 질문루프1~2 + 결과패널1~2 + 마무리1 = 약 6~9 작업 세션. ③추출실험 후 카나나/큐웬 판정, 단계별 부분 배포 가능.
   - **개인정보 설명 자료**: 상담 내용은 서버 부재로 유출 경로 없음(localhost·브라우저 내) — 배포 안내문에 쓸 데이터 흐름표를 2026-07-20 대화에서 정리함.
3. **[소소한 후속] Ollama 경로에도 예산 가드 확장 검토** — 현재 가드는 카나나 전용. Ollama는 numCtx 8192(2배)라 여유가 크지만 무한은 아니어서, 극단적으로 긴 대화면 역시 잘릴 수 있다. 트랙 2가 완성되면 자연 해소되므로 급하지 않음 — 트랙 2 지연 시에만 착수.
4. **[확인만] 배포 사이트 속도 체감 비교** — 멀티스레드 적용 전후 tokens/sec 또는 체감. 사용자 보고 기준.

## 5. 근거 요약 (한 줄씩)

- 잘림·크래시의 원인은 출력 상한이 아니라 문맥 예산 — nPredict를 올려도 잘린다는 실측이 이미 코드 주석에 있다.
- 공책(4096칸)을 못 키우니(8192 실측 기각) 문제를 짧게 쓰는 것만 남는다.
- 작은 모델은 기억·계산이 약하다 — 계산은 코드가, 모델은 읽고 쓰는 일만 시켜야 정확해진다.
- 속도는 일꾼 수(스레드)와 읽을 양(프롬프트 길이)의 문제 — 둘 다 모델 교체 없이 줄일 수 있다.
